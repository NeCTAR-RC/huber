---
features:
  - |
    Added support for error reporting to GlitchTip/Sentry. Set ``dsn``
    (and optionally ``environment``) in the ``[sentry]`` section of the
    config file, or the ``SENTRY_DSN`` environment variable, to report
    unhandled exceptions and ERROR level log messages.
