---
other:
  - |
    Release notes are now managed with `reno
    <https://docs.openstack.org/reno/latest/>`_. Add a note for any
    user-facing change with ``reno new <slug>`` and edit the generated YAML
    file under ``releasenotes/notes/``.
